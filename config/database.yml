default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  port: <%= ENV.fetch("DATABASE_PORT", 5432) %>

development:
  <<: *default
  database: air_tickets_api_development
  username: <%= ENV.fetch("DATABASE_USERNAME", "air_tickets_api") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "007jand9000") %>
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>

test:
  <<: *default
  database: air_tickets_api_test
  username: <%= ENV.fetch("DATABASE_USERNAME", "air_tickets_api") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "007jand9000") %>
  host: <%= ENV.fetch("DATABASE_HOST", "localhost") %>

production:
  <<: *default
  url: <%= ENV.fetch("DATABASE_URL") %>
